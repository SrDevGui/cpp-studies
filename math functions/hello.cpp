#include <iostream>
#include <cmath>

int main(){
  double x = 3;
  double y = 4;
  double t = 4.14;
  double z;

  // z = std::max(x,y); -> 4
  // z = std::min(x,y); -> 3
  // z = pow(2,3); ->8
  // z = sqrt(9); ->3
  // z = abs(-3); ->3
  // z = round(t); ->4
  // z = ceil(t); ->5
  // z = floor(t); ->4
  
  std::cout <<z;
  return 0;
}